# Port for the dev server (python run.py). Gunicorn uses PORT too inside Docker.
PORT=5000

# Base URL of the Open-Meteo forecast API (override only if you self-proxy it).
OPEN_METEO_URL=https://api.open-meteo.com/v1/forecast

# Cache lifetime for weather responses, in minutes.
CACHE_TTL_MINUTES=30
